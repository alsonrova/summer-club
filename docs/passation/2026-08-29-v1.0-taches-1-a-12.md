# Passation — V1.0, tâches 1 à 12

**Date** : 2026-08-29 · **Branche** : `v1.0` (pour la tête exacte : `git log -1`)

Remplace `docs/passation/2026-08-27-v1.0-taches-1-a-11.md`, conservée pour l'histoire.

## Où on en est

| Tâche | Sujet | État |
|---|---|---|
| 1 à 10 | Socle, domaine pur, données, authentification, moteur d'admin | ✅ validées |
| 11 | Écrans produits, déclinaisons, stock, photos | ✅ validée |
| 12 | Écrans commandes (statuts) et avis | ✅ validée (3 workflows : implémentation, 3 relectures adverses, 2 passes de correction) |
| 13 à 23 | Vitrine côté clientes, paiement, déploiement | à faire |

**Suite verte, `tsc --noEmit` propre, build de production réussi.**

Le nombre de tests n'est **pas figé ici**, et c'est délibéré : cette page a d'abord annoncé
« 222 tests Vitest verts (22 fichiers) » dans le commit même qui en portait 240 sur 23 —
un document versionné avec le changement qu'il décrit est toujours en retard d'un cran, pour
un compteur comme pour un SHA. Pour la valeur courante, la commande fait foi :

```bash
npm test                          # nombre de tests Vitest et de fichiers
npx --no-install playwright test  # tests de bout en bout (exige npm run build avant)
```

Dernière mesure connue, à titre d'ordre de grandeur et non d'état courant : **240 tests
Vitest sur 23 fichiers**, mesurés le 2026-08-30. Le journal (`docs/journal/`) garde les
chiffres datés tâche par tâche — c'est lui qui sert de référence historique, pas cette page.

Le back-office a ses trois écrans. **La vitrine côté clientes n'existe pas encore** : elle
arrive aux tâches 13 à 15.

## Nouveau : les conventions sont écrites

Le propriétaire a fixé cinq règles, désormais consignées dans **`docs/CONVENTIONS.md`**, qui
fait autorité sur ce dépôt. `CLAUDE.md` y renvoie en première ligne. En résumé :

1. **Le projet est indépendant** : aucune norme d'un autre dépôt ne s'applique ici d'office.
2. **Identifiants en anglais, tout ce que lit un humain en français** — libellés, messages
   d'erreur, commentaires, commits, documentation. Deux précisions qui décident de cas réels :
   **les segments de route sous `src/app/` sont en français** parce qu'une URL est lue par un
   être humain (`admin/produits`, `connexion`, et demain `boutique`, `panier`) ; **les noms
   d'énumération et leurs valeurs sont en anglais**, y compris en base. Le renommage du code
   existant est un **travail séparé** : ne renommez rien en passant.
3. **Architecture en quatre couches** (`domain` / `server` / `admin` / `app`), une couche ne
   connaît que celles en dessous d'elle. SOLID illustré par du code réel de ce dépôt.
4. **Quatre rôles d'agents** : développeur, auditeur qualité et sécurité, testeur UX/UI,
   coordinateur — avec ce que chacun doit produire.
5. **Un journal des agents versionné** : `docs/journal/`, outil dans `tools/agent-journal/`.

Deux règles d'orchestration à ne pas perdre :

- **Les agents de vérification travaillent en lecture seule.** Aucune modification de fichier,
  même temporaire — journal compris. Une vérification par mutation se fait **seule et
  séquentiellement**. Un vérificateur remet son entrée de journal au coordinateur sous forme
  de ligne de commande prête à exécuter ; c'est le coordinateur qui l'inscrit.
- **Ne citez jamais un identifiant de commit pour désigner l'état courant.** Un document
  versionné avec le changement qu'il décrit sera toujours en retard d'un cran.

## Le journal des agents

```bash
npm run journal -- list --task 12      # ce qui s'est passé sur une tâche
npm run journal -- add --task 13 --role developer --summary "…" --verdict delivered
npm run journal:render                 # met docs/journal/JOURNAL.md à jour
```

Mode d'emploi : `docs/journal/README.md`. Il est amorcé avec l'histoire réelle des douze
tâches livrées (entrées marquées `reconstructed` : horodatages calés sur des commits réels).
**Consignez votre intervention en fin de tâche** — c'est la seule mémoire entre sessions. Si
vous êtes en lecture seule (auditeur, testeur UX/UI), n'exécutez pas la commande : remettez-la
au coordinateur, rédigée et prête à coller.

## À reprendre en priorité

Rien de bloquant. La suite reprend à la **tâche 13** (vitrine).

## Pièges de l'environnement

- **Le conteneur PostgreSQL s'arrête tout seul** (instabilité Docker Desktop). Symptôme
  trompeur : une vingtaine de tests échouent d'un coup comme s'il y avait une régression.
  Réflexe : `docker exec docker-db-1 pg_isready -U summerclub -d summerclub`, puis
  `docker compose -f docker/compose.dev.yml up -d`.
- **`next dev` ne sert rien sur cette machine** : il démarre, n'émet aucun log, ne répond
  jamais. Playwright vise `npm run start` ; un `npm run build` doit donc précéder.
- **`npm install` prend 10 à 20 minutes** et ne doit jamais être lancé deux fois en parallèle
  (`node_modules` corrompu deux fois). Windows Defender analysant `node_modules` est le
  suspect principal.
- **Prisma épinglé en 6.19.3** ; la v7 supprime `url = env("DATABASE_URL")` dans le schéma.
- **zod est en version 4** : `_def.type`, et non `_def.typeName` comme en v3.
- `prisma migrate dev` échoue sans terminal interactif dès qu'il doit avertir. Contournement :
  `prisma migrate diff` puis `prisma migrate deploy`.
- Node exécute le TypeScript nativement (`node --experimental-strip-types`) : `tsx` est inutile.
- **`AGENTS.md` est réécrit par `next dev`** : ne comptez pas dessus, le point d'entrée durable
  est `CLAUDE.md`.
- Configuration du serveur de prévisualisation : `D:\Projet\.claude\launch.json`, entrée
  `summerclub`.

## Décisions du propriétaire

- **Orange Money réserve le stock dès la création** (l'argent est en vol) ; WhatsApp ne réserve
  pas, mais sa confirmation contrôle le stock. D'où `en_attente_paiement` dans `STOCK_ENGAGE`.
- **`expediee → annulee` recrédite le stock immédiatement**, choix assumé pour une boutique qui
  livre elle-même.

## Ce que la tâche 12 a livré, et ce qu'elle laisse

`appliquerStatut` (`src/server/order-status-service.ts`) est le **cœur métier sans
authentification** : le webhook de paiement de la tâche 19 l'appellera tel quel.
`changerStatut` (`src/app/admin/commandes/actions.ts`) en est l'enveloppe authentifiée, et
c'est elle qui invalide les caches en suivant la liste publiée par `cheminsARevalider(orderId)`
— la tâche 19 n'aura rien à deviner.

Réserves encore ouvertes :

- Gardes d'idempotence des avis lus **hors** transaction (acceptable sur un écran
  mono-utilisatrice, pas au-delà).
- Intermittence de bout en bout vue **une fois** (bouton bloqué sur « Enregistrement… »
  30 s), non reproduite en quatre exécutions. Si elle revient, chercher du côté de la
  revalidation concurrente de la page statique `/` — **et pas du côté d'un retry ou d'une
  sérialisation**.
- `id` et `orderId` volontairement non validés dans les actions de statut : décision
  documentée dans le code.
- `tests/server/orders.test.ts` purge au DÉBUT de chaque test : il laisse donc une commande
  et `VAH-45` à un stock modifié derrière lui. Antérieur à la tâche 12.
- **`tests/admin/produits-query.test.ts` échoue sur « Hook timed out in 10000ms » quand la
  machine est chargée.** Observé le 2026-08-30, une fois sur six exécutions complètes, juste
  après un `npm run build` : 8 fichiers en échec d'un coup, symptôme trompeur d'une
  régression. Le conteneur était pourtant `accepting connections` et à 44 h d'uptime. Cause
  identifiée, non corrigée : son `beforeAll` crée **25 produits en 25 allers-retours
  séquentiels** (`PRODUITS_PAR_PAGE + 5`), sous le délai de garde par défaut de 10 s de
  Vitest — le fichier seul met 16 s à froid contre 6,5 s à chaud. La correction est de
  **créer les 25 lignes en un seul appel** (`createMany`), pas de relever le délai ni
  d'ajouter un retry (§ 6.3). Antérieur à cette passation.

## Dettes ouvertes

- **Backlog V1.1** : conserver `promotionId` et `prixInitial` sur `OrderItem` — impossible
  aujourd'hui de savoir a posteriori quelle promotion a servi.
- **Backlog V1.2** : la table `expenses` de la spec §5 n'appartient à aucune tâche V1.0.
- **Tâche 17** : l'interface `PaymentProvider` n'existe que dans le plan. C'est elle qui
  portera l'inversion de dépendance du paiement.
- **Tâche 22** : `advanced.ipAddress` de Better Auth reste à configurer avec Caddy — sans quoi
  le seau de limitation de débit est global et un visiteur anonyme peut verrouiller la
  connexion de l'administratrice. `next start` ignore par ailleurs `output: 'standalone'`
  (simple avertissement, le build normal est servi) : pour la sortie standalone réelle, lancer
  `node .next/standalone/server.js`.
- **Renommage des identifiants français** vers l'anglais, gouverné par `docs/CONVENTIONS.md`
  § 1. Touche aussi le code d'exemple de la spec et du plan, qui portent un avertissement en
  tête. Travail à part entière, à ne pas mélanger à une tâche fonctionnelle. Le lot inclut
  **les sept énumérations Prisma et leurs valeurs** (`StatutCommande`, `StatutAvis`,
  `SourceAvis`, `Canal`, `TypePromo`, `PortePromo`, `Role`) — donc une migration sur des
  données déjà écrites, la partie la plus délicate. Il **exclut les segments de route** :
  `admin/produits` est conforme, pas en retard.
- Pas d'écran catégories : en créer une passe encore par la base.

## Protocole

Exécution pilotée par sous-agents, un agent par tâche, revue systématique après chaque,
correctifs puis re-revue jusqu'à validation. Rôles et livrables attendus :
`docs/CONVENTIONS.md` § 8. Journal : `docs/journal/`. Ledger de session :
`.superpowers/sdd/progress.md` (**non versionné**, ne survit pas à la machine). Plan :
`docs/superpowers/plans/2026-08-12-summerclub-v1.0.md`. Spec :
`docs/superpowers/specs/2026-08-12-summerclub-boutique-design.md`.
